# Issue Tracker

Issues live in the repo's GitHub Issues: `https://github.com/Qinqing1999/nanobot/issues`

Uses the `gh` CLI to create and manage issues.

## PRs as a request surface

Off. External PRs are not automatically added to the triage queue.
